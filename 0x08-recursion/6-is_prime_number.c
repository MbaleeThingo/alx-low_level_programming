#include "main.h"

int is_divisible(int num, int div);
int is_prime_number(int n);

/**
* is_divisible - checks if no is divisible
* @num: the no to be checked
* @div: the divisor
* Return: 0 if the no is divisible
* 1 if the no is not divisible
*/

int is_divisible(int num, int div)
{
if (num % div == 0)
return (0);
}
